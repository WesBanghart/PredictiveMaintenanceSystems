import React from 'react'
import Typography from '@material-ui/core/Typography'

const Scheduling: React.FC = () => {
    return (
        <Typography variant="h3" component="h1">Scheduling Page</Typography>
    )
};

export default Scheduling;