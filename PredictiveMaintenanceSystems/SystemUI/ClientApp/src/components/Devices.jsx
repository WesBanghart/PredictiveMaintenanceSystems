import React from 'react'
import Typography from '@material-ui/core/Typography'

//OLD devices page
const Devices: React.FC = () => {
    return (
        <Typography variant="h3" component="h1">Devices Page</Typography>
    )
};

export default Devices;